//const new_ingredient = document.getElementById("select2-dose_ingredient_id-container").innerText;
//const description = document.getElementById("dose_description").value;
//const ingredients = document.querySelector(".cocktail-ingredients");
//const submit = document.getElementById("btn-add-ingredient");

//submit.addEventListener('click', (event) => {
//  ingredients.insertAdjacentHTML("beforeend", `<p>${new_ingredient}: ${description}</p>`);
//});
